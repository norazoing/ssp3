// fgets() reads in at most one less than size characters from stream
// and stores them into the buffer pinted to by s. Reading stops after an EOF or a newline.
// if a newline is read, it is stored into the buffer.
// A terminating null byte is sotred after the last character in the buffer
